def average(array):
    # your code goes here
    array_sum=sum(set(array))
    array_length=len(set(array))
    ans=array_sum/array_length
    return ans

if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    result = average(arr)
    print(result)
